// Make sure the Header component exists at the specified path, or update the path if necessary
// Update the import path below to the correct location of Header component
// Example: import { Header } from "../components/Header";
import { Header } from "../components/Header";

export default function Profile() {
    return (
        <>
        <Header />
        <main>

        </main>
        </>
    );
}